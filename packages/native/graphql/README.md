# GraphQL

GraphQL queries and mutations for the app.

## Structure

- `queries/` - GraphQL queries for fetching data
- `mutations/` - GraphQL mutations for data modifications

## Code Generation

Use `npm run codegen` to generate TypeScript types from GraphQL schema.
